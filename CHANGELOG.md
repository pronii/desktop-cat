# Changelog

## 0.3.9

- 新增设备授权与管理后台能力，服务端支持生成授权码、绑定设备、记录授权检查，并提供受 token 保护的浏览器管理页。
- 打包时支持通过环境变量注入线上好友同屏服务地址，避免把部署 IP 写进源码，同时保留本地开发默认地址。
- 强化自动更新发布链路，GitHub Release 会上传安装包、portable 包、`latest.yml`、`.blockmap` 和 `SHA256SUMS.txt`，用于安装版全量更新下载。
- 优化房间面板记忆、全屏置顶恢复、拖拽生命周期和服务端授权接口测试覆盖，降低更新后交互状态丢失和窗口显示异常的风险。

## 0.3.8

- 好友同屏不再同步或依赖好友自己的屏幕坐标，本地只按当前角色旁边的固定侧边位置展示好友。
- 好友 Live2D 和小猫窗口改为与本地角色底部对齐，缩小后也能保持像站在同一条地面线上。
- 保持好友窗口之间的固定水平距离，并补充回归测试覆盖固定间距、底部对齐和坐标无关布局。

## 0.3.7

- 修复本地 Live2D 缩小后，好友同屏形象没有贴近本地角色站位的问题；好友窗口现在会按当前 `scale` 重算可见角色锚点。
- 房间同步会保留本地宠物的 `scale` 字段，确保跨端好友同屏能拿到尺寸信息。
- 更新提示会把 GitHub Release 的 HTML 更新说明转换成可读纯文本，避免在应用内直接显示标签和实体字符。

## 0.3.6

- 好友同屏现在会同步本地宠物的 `appearanceType`、`modelId` 和 `modelName`，让房间里的好友优先显示对应 Live2D 形象。
- 好友窗口支持加载本地匹配的 Live2D 模型，缺失或加载失败时会依次回退到默认 Live2D 模型和原小猫形象。
- 限制好友 Live2D 渲染窗口数量并补充回归测试，降低多人同屏时的资源占用和异步加载串台风险。

## 0.3.5

- 调整鼓励提示 `happy-bubble` 的垂直位置，让文案显示在角色上方，避免遮挡底部工具栏。
- 将 `happy-bubble` 顶部偏移固定为 `-28px`，并补充对应回归测试，防止提示气泡回退到角色内部。

## 0.3.4

- 自动更新下载改为完整下载，避开 GitHub Release 差分更新中的 blockmap 和分段请求失败面。
- 自动更新失败时显示可操作提示，并提供“打开下载页”入口，方便用户手动下载安装包。
- 补充更新器失败兜底和完整下载策略的回归测试。

## 0.3.3

- 重绘 Live2D 形象更换按钮，将原本过密的剧场面具图标替换为更清晰的 `appearance-sparkles` 头像星光图标。
- 保留形象更换按钮原有的圆形白底、棕色描边、阴影、尺寸和位置，仅替换内部 SVG 图形。
- 补充图标回归测试，防止形象更换按钮退回旧的高密度图标。

## 0.3.2

- 接入 `electron-updater`，让 NSIS 安装版可以通过 GitHub Releases、`latest.yml` 和 `.blockmap` 检查并下载新版本。
- 保留现有应用内更新提示，更新可用时先让用户确认下载，下载完成后再提示重启安装。
- 为安装版自动更新补充 GitHub publish 打包配置和回归测试。
- 保留原有 `latest.json` 远程更新通道，用于服务端推送提示、调试和便携版场景。

## 0.3.1

- 优化桌面悬浮控件的 UI/UX，提升键盘焦点、按钮命中区域和弹窗交互体验。
- 将底部工具栏和面板中的结构性 emoji 图标替换为本地内联 Lucide SVG 图标，并补充 Lucide 许可说明。
- 删除旧版小猫喝水动画，仅保留喝水记录、提醒和稍后提醒等工具功能。
- 调整小猫大小按钮的箭头方向，使图标朝左，同时保留按钮原有背景、描边、阴影和布局。
- 强化 Live2D 切换、拖拽释放、剪贴板反馈和更新提示的回归测试覆盖。
